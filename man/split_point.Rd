% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/treeBuilding.R
\name{split_point}
\alias{split_point}
\title{Compute a split point for a numeric vector}
\usage{
split_point(X, strategy = "midpoint", quantile_range = c(0.1, 0.9))
}
\arguments{
\item{X}{A numeric vector of covariate values in the current node.}

\item{strategy}{Character; either \code{"midpoint"} or
\code{"random_quantile"}. Default \code{"midpoint"}.}

\item{quantile_range}{Numeric vector of length 2 giving the lower and
upper bounds of the quantile range when
\code{strategy = "random_quantile"}. Default \code{c(0.1, 0.9)}.}
}
\value{
Numeric scalar split point, or \code{NA_real_} if no finite
values exist.
}
\description{
Selects a split point according to one of two strategies:
\describe{
\item{\code{"midpoint"}}{The classic \code{(min + max) / 2} rule.
Deterministic given the data in the node.}
\item{\code{"random_quantile"}}{Draws a uniform random quantile in
\code{[quantile_range[1], quantile_range[2]]} (default 10th–90th
percentile) and returns the corresponding data quantile. This
introduces controlled stochasticity into split-point selection,
improving coverage of the covariate space across a forest of trees.}
}
}
\keyword{internal}
